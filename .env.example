# Exemple de configuration (NE PAS mettre de secrets dans le repo)
# Copiez ce fichier en .env puis remplissez les valeurs.

# ==========================
# IA (OpenAI)
# ==========================
# Requis pour l'analyse IA et la génération de scripts
OPENAI_API_KEY=your_openai_api_key_here

# Modèle utilisé (défaut dans le projet: gpt-3.5-turbo)
OPENAI_MODEL=gpt-3.5-turbo

# Contrôle des coûts/latences (valeurs typiques)
MAX_TOKENS=1000
TEMPERATURE=0.3

# ==========================
# Scan (Nmap)
# ==========================
# Timeout par défaut des scans (secondes)
SCAN_TIMEOUT=300

# Arguments Nmap par défaut (si vous utilisez le mode scan)
NMAP_DEFAULT_ARGS=-sV -sC --script vuln

# ==========================
# Limites (économie de tokens)
# ==========================
MAX_VULNERABILITIES_TO_ANALYZE=10
MAX_SCRIPTS_TO_GENERATE=5

# ==========================
# Stockage / logs
# ==========================
DATABASE_PATH=data/database/vulnerability_agent.db
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
